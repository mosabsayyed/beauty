# Observability Dashboard - Visual Reference

## Backend API Endpoints

| Endpoint | Method | Description |
|----------|--------|-------------|
| `/api/v1/debug/traces` | GET | List all conversation traces (limit=50) |
| `/api/v1/debug/traces/{conversation_id}` | GET | Get full trace details |
| `/api/v1/debug/prompts/toggle` | POST | Enable/disable debug mode |
| `/api/v1/debug/prompts/status` | GET | Check debug mode status |
| `/api/v1/debug/database-config` | GET | Check database configuration |

**Base URL:** `http://localhost:8008`

**Source File:** `/backend/app/api/routes/debug.py`

---

## UI Layout

```
┌─────────────────────────────────────────────────────────────────────────────────────┐
│  ◎ Observability Dashboard                                        [Back to Chat]   │
│    Monitor LLM reasoning, tool calls, and cognitive flow                            │
├──────────────────────┬──────────────────────────────────────────────────────────────┤
│  TRACE LIST          │  TRACE DETAIL                                                │
│  ────────────────    │  ────────────────────────────────────────────────────────    │
│                      │                                                               │
│  ┌────────────────┐  │  ◀ Trace #374                        [3 Tool Calls] [Success]│
│  │ #374  ✓ maestro│  │  2025-12-13 14:30:45                                         │
│  │ What is the... │  │                                                               │
│  │ ⚡3 tools  🕐..│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐             │
│  └────────────────┘  │  │ 2 Turns │ │ 3 Tools │ │ 1 Reason│ │ 0 Errors│             │
│                      │  └─────────┘ └─────────┘ └─────────┘ └─────────┘             │
│  ┌────────────────┐  │                                                               │
│  │ #373  ✗ noor  │  │  ┌──────────┬───────────┬────────────┬────────┬──────────┐   │
│  │ Show me the...│  │  │ Timeline │ Reasoning │ Tool Calls │ Errors │ Raw JSON │   │
│  │ ⚡0 tools  🕐..│  │  └──────────┴───────────┴────────────┴────────┴──────────┘   │
│  └────────────────┘  │                                                               │
│                      │  ┌──────────────────────────────────────────────────────┐    │
│  ┌────────────────┐  │  │ ▶ tier1_loaded           [layer2]      14:30:45      │    │
│  │ #372  ✓ maestro│  │  └──────────────────────────────────────────────────────┘    │
│  │ Calculate...  │  │  ┌──────────────────────────────────────────────────────┐    │
│  │ ⚡5 tools  🕐..│  │  │ ▼ groq_full_trace        [layer2]      14:30:48      │    │
│  └────────────────┘  │  │ ───────────────────────────────────────────────────  │    │
│                      │  │ {                                                     │    │
│       ⟳ Refresh     │  │   "tool_calls_count": 3,                              │    │
│                      │  │   "tool_calls": [...],                                │    │
│                      │  │   ...                                                 │    │
│                      │  │ }                                                     │    │
│                      │  └──────────────────────────────────────────────────────┘    │
│                      │                                                               │
├──────────────────────┴──────────────────────────────────────────────────────────────┤
│                           320px                    flexible                          │
└─────────────────────────────────────────────────────────────────────────────────────┘
```

---

## Color Scheme

### Dark Theme (Default)

| Element | Color | CSS Variable |
|---------|-------|--------------|
| Page Background | `#111827` | `--component-bg-primary` |
| Panel Background | `#1F2937` | `--component-panel-bg` |
| Card Background | `#374151` | `--component-bg-secondary` |
| Primary Text | `#F9FAFB` | `--component-text-primary` |
| Secondary Text | `#D1D5DB` | `--component-text-secondary` |
| Muted Text | `#9CA3AF` | `--component-text-muted` |
| Accent (Gold) | `#FFD700` | `--component-text-accent` |
| Success (Green) | `#10B981` | `--component-color-success` |
| Error (Red) | `#EF4444` | `--component-color-danger` |
| Border | `#374151` | `--component-panel-border` |

### Light Theme

| Element | Color | CSS Variable |
|---------|-------|--------------|
| Page Background | `#F3F4F6` | `--component-bg-primary` |
| Panel Background | `#FFFFFF` | `--component-panel-bg` |
| Primary Text | `#1F2937` | `--component-text-primary` |
| Accent (Amber) | `#D97706` | `--component-text-accent` |

---

## Component Visual Breakdown

### 1. Header Bar

```
┌───────────────────────────────────────────────────────────────────────────┐
│  👁️  Observability Dashboard                            [Back to Chat]   │
│      Monitor LLM reasoning, tool calls, and cognitive flow               │
└───────────────────────────────────────────────────────────────────────────┘
     ▲                ▲                                         ▲
     │                │                                         │
  Icon (gold)    Title + Subtitle                          Button (outline)
```

**CSS Classes:**
- `.observability-header` - Container
- `.observability-header-icon` - Eye icon (gold accent)
- `.observability-header-title` - Main title
- `.observability-header-subtitle` - Description text
- `.observability-back-btn` - Navigation button

---

### 2. Trace List Panel

```
┌─────────────────────────┐
│ 📑 Conversation Traces ⟳│ ← Header with refresh
├─────────────────────────┤
│ ┌─────────────────────┐ │
│ │ #374     ✓  maestro │ │ ← Selected state (blue bg)
│ │ What is the rev...  │ │
│ │ ⚡3 tools  🕐 14:30 │ │
│ └─────────────────────┘ │
│ ┌─────────────────────┐ │
│ │ #373     ✗  noor    │ │ ← Error indicator (red icon)
│ │ Show me the data... │ │
│ │ ⚡0 tools  🕐 14:25 │ │
│ └─────────────────────┘ │
│         ...             │
└─────────────────────────┘
       320px width
```

**Trace Item States:**
| State | Visual Indicator |
|-------|------------------|
| Default | Transparent background |
| Hover | `--component-bg-secondary` background |
| Selected | Blue tint + gold left border |
| Has Error | Red AlertCircle icon |
| Success | Green CheckCircle icon |

**CSS Classes:**
- `.trace-list-panel` - Container
- `.trace-list-header` - Header bar
- `.trace-list-content` - Scrollable list
- `.trace-item` - Individual trace card
- `.trace-item.selected` - Selected state
- `.status-icon.success` / `.status-icon.error`

---

### 3. Summary Cards

```
┌───────────┐ ┌───────────┐ ┌───────────┐ ┌───────────┐
│ 💬        │ │ ⚡        │ │ 🧠        │ │ ⚠️        │
│ 2         │ │ 3         │ │ 1         │ │ 0         │
│ Turns     │ │ Tool Calls│ │ Reasoning │ │ Errors    │
└───────────┘ └───────────┘ └───────────┘ └───────────┘
    Blue         Green        Purple         Red
```

**CSS Classes:**
- `.summary-cards` - Grid container (4 columns)
- `.summary-card` - Individual card
- `.summary-card-icon.blue/green/purple/red`
- `.summary-card-value` - Large number
- `.summary-card-label` - Description text

---

### 4. Tab Navigation

```
┌──────────┬───────────┬────────────┬────────┬──────────┐
│ Timeline │ Reasoning │ Tool Calls │ Errors │ Raw JSON │
│   🕐     │    🧠     │     ⚡     │   ⚠️   │    </>   │
└──────────┴───────────┴────────────┴────────┴──────────┘
     ▲
   Active (gold underline + text)
```

**Tab States:**
| State | Visual |
|-------|--------|
| Default | Muted text, no underline |
| Hover | Primary text, secondary bg |
| Active | Gold text, gold bottom border |

**CSS Classes:**
- `.tabs-container` - Wrapper
- `.tabs-list` - Tab bar
- `.tab-trigger` - Individual tab
- `.tab-trigger.active` - Active state
- `.tab-content` - Content area

---

### 5. Timeline Events

```
┌─────────────────────────────────────────────────────────────┐
│ 🗄️ tier1_loaded        [layer2]             14:30:45    ▶  │ ← Collapsed
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ 💚 groq_full_trace     [layer2]             14:30:48    ▼  │ ← Expanded
├─────────────────────────────────────────────────────────────┤
│ {                                                           │
│   "tool_calls_count": 3,                                    │
│   "tool_calls": [                                           │
│     { "tool": "read_neo4j_cypher", ... }                   │
│   ],                                                        │
│   ...                                                       │
│ }                                                           │
└─────────────────────────────────────────────────────────────┘
```

**Event Type Colors:**
| Type Pattern | Icon Color | Border Color |
|--------------|------------|--------------|
| `*error*`, `*failed*` | Red | Red |
| `groq_full_trace` | Green | Green |
| `tier1*` | Blue | Blue |
| Default | Gray | Gray |

**CSS Classes:**
- `.timeline-event` - Container
- `.timeline-event.error/success/tier1`
- `.timeline-event-header` - Clickable header
- `.timeline-event-data` - Expandable content
- `.code-block` - JSON display

---

### 6. Tool Calls Panel

```
┌─────────────────────────────────────────────────────────────┐
│ 🗄️ MCP Tools Available                                      │
├─────────────────────────────────────────────────────────────┤
│ [read_neo4j_cypher] [write_neo4j_cypher] [query_supabase]  │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│ ⚡ read_neo4j_cypher                       [mcp_router]     │ ← Green left border
├─────────────────────────────────────────────────────────────┤
│ {                                                           │
│   "query": "MATCH (n:Entity) RETURN n LIMIT 10"            │
│ }                                                           │
└─────────────────────────────────────────────────────────────┘
```

**CSS Classes:**
- `.mcp-tools-card` - Available tools list
- `.mcp-tools-list` - Badge container
- `.tool-call-card` - Individual tool call
- `.tool-call-header` - Tool name + server badge
- `.tool-call-content` - Arguments JSON

---

### 7. Reasoning Panel

```
┌─────────────────────────────────────────────────────────────┐
│ 🧠 Reasoning Step 1                                         │ ← Purple left border
├─────────────────────────────────────────────────────────────┤
│ ┌─────────────────────────────────────────────────────────┐ │
│ │ I need to query the Neo4j database to understand the   │ │
│ │ relationship between revenue entities. Let me construct │ │
│ │ a Cypher query that will retrieve the relevant nodes... │ │
│ └─────────────────────────────────────────────────────────┘ │
└─────────────────────────────────────────────────────────────┘
```

**CSS Classes:**
- `.reasoning-step` - Container (purple left border)
- `.reasoning-step-header` - Step number
- `.reasoning-step-content` - Content wrapper
- `.reasoning-thought` - Purple tinted thought bubble

---

### 8. Errors Panel

**No Errors State:**
```
┌─────────────────────────────────────────────────────────────┐
│                           ✓                                 │
│                    No errors detected                       │
│                      (green text)                           │
└─────────────────────────────────────────────────────────────┘
```

**Error Card:**
```
┌─────────────────────────────────────────────────────────────┐
│ ⚠️ json_parse_failed                                        │ ← Red border + tint
├─────────────────────────────────────────────────────────────┤
│ {                                                           │
│   "raw_output": "Invalid JSON...",                          │
│   "error": "Expecting value: line 1 column 1"              │
│ }                                                           │
└─────────────────────────────────────────────────────────────┘
```

**CSS Classes:**
- `.errors-success` - Success state (green)
- `.error-card` - Error container (red theme)
- `.error-card-header` - Error type
- `.error-code-block` - Error details

---

### 9. Empty State (No Trace Selected)

```
┌─────────────────────────────────────────────────────────────┐
│                                                             │
│                         👁️                                  │
│                   (large, 30% opacity)                      │
│                                                             │
│                   Select a Trace                            │
│                                                             │
│        Click on a conversation trace to view its            │
│        full execution details, including reasoning          │
│        steps, tool calls, and errors.                       │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**CSS Classes:**
- `.detail-empty` - Centered container
- `.detail-empty-icon` - Large faded icon
- `.detail-empty h3` - Title
- `.detail-empty p` - Description

---

## Responsive Behavior

### Desktop (>1024px)
- Side-by-side layout (320px list + flex detail)
- 4-column summary cards

### Tablet (768px - 1024px)
- List panel collapses
- 2-column summary cards

### Mobile (<768px)
- Stacked layout
- 1-column summary cards
- Horizontal scrolling tabs

---

## Icon Reference

| Icon | Component | Meaning |
|------|-----------|---------|
| 👁️ Eye | Header, Empty State | Observability |
| 📑 Layers | Trace List | Conversation stack |
| ⟳ RefreshCw | Trace List | Refresh button |
| ✓ CheckCircle | Trace Item | Success |
| ⚠️ AlertCircle | Trace Item, Errors | Error/Warning |
| 🕐 Clock | Timeline, Meta | Time/Duration |
| ⚡ Zap | Tool Calls, Summary | Tool/Action |
| 🧠 Brain | Reasoning, Summary | LLM Thinking |
| 💬 MessageSquare | Summary | Conversation turns |
| 💻 Cpu | Timeline | Groq/LLM events |
| 🗄️ Database | Timeline, MCP | Database/MCP events |
| </> Code | Timeline, Raw | Code/Parse events |
| ◀ ArrowLeft | Detail Header | Back navigation |
| ▶▼ Chevron | Timeline | Expand/Collapse |

---

## Badge Variants

| Variant | Usage | Colors |
|---------|-------|--------|
| `badge-default` | Maestro persona | Gold bg, dark text |
| `badge-secondary` | Noor persona | Gray bg, light text |
| `badge-outline` | Layers, tools | Transparent, border |
| `badge-success` | Success status | Green bg, white text |
| `badge-destructive` | Error status | Red bg, white text |
